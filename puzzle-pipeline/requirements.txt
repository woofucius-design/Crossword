# Pipeline dependencies. A fresh container has none of these:
#   pip install -r requirements.txt && python3 -c "import nltk; [nltk.download(p, quiet=True) for p in ('averaged_perceptron_tagger','averaged_perceptron_tagger_eng','punkt','punkt_tab')]"
lemminflect>=0.2.3
nltk>=3.8
