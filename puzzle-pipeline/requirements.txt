# Puzzle-pipeline Python dependencies.
#
# Clue generation (enrich_definitions.py) needs:
#   lemminflect  — inflect a root word's definition to match the answer's
#                  form (wear -> wore/wears/wearing; person -> persons),
#                  handles irregulars.
#   nltk         — POS tagging to locate the head verb / head noun of a
#                  definition so the right word gets inflected.
#                  Also requires the tagger model, downloaded on first run:
#                    python3 -c "import nltk; nltk.download('averaged_perceptron_tagger_eng')"
#                  (enrich_definitions.py attempts this automatically.)
lemminflect>=0.2.3
nltk>=3.9
