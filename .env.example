# Supabase — leave blank to run fully offline with demo data + AI fallbacks.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Edge-function secret (set in the Supabase dashboard, NOT in the app build).
# ANTHROPIC_API_KEY=
